<div class="containerProd">
    {{#each listaProds}}

    <div class="cardProd">
    <h3>{{this.title}}</h3>
    <p>{{this.description}}</p>
    <p>Precio: ${{this.price}}</p>
    <p>Stock: {{this.stock}}</p>

    </div>
    {{/each}}
</div>